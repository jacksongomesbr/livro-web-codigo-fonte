<IfModule mod_rewrite.c>
    Options -MultiViews

    RewriteBase /livro-web-codigo-fonte/backend/acessos-analytics/api
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [QSA,L]
</IfModule>
